
{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "description": "Profil pengguna Linku.",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "username": { "type": "string" },
        "displayName": { "type": "string" },
        "bio": { "type": "string" },
        "avatarUrl": { "type": "string" },
        "role": { "type": "string", "enum": ["User", "Admin"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "email", "username", "role"]
    },
    "LinkGroup": {
      "title": "LinkGroup",
      "type": "object",
      "description": "Kelompok atau folder tautan.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "title": { "type": "string" },
        "imageUrl": { "type": "string" },
        "order": { "type": "number" },
        "isEnabled": { "type": "boolean" }
      },
      "required": ["id", "userId", "title", "isEnabled"]
    },
    "Link": {
      "title": "Link",
      "type": "object",
      "description": "Tautan individu.",
      "properties": {
        "id": { "type": "string" },
        "groupId": { "type": "string", "description": "Optional, jika masuk kelompok" },
        "title": { "type": "string" },
        "url": { "type": "string" },
        "imageUrl": { "type": "string" },
        "order": { "type": "number" },
        "isEnabled": { "type": "boolean" },
        "clicks": { "type": "number" }
      },
      "required": ["id", "title", "url", "isEnabled"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/userProfiles/{userId}": {
      "schema": { "$ref": "#/backend/entities/UserProfile" },
      "description": "Profil pengguna."
    },
    "/userProfiles/{userId}/links/{linkId}": {
      "schema": { "$ref": "#/backend/entities/Link" },
      "description": "Tautan mandiri milik pengguna."
    },
    "/userProfiles/{userId}/linkGroups/{groupId}": {
      "schema": { "$ref": "#/backend/entities/LinkGroup" },
      "description": "Kelompok link milik pengguna."
    },
    "/userProfiles/{userId}/linkGroups/{groupId}/links/{linkId}": {
      "schema": { "$ref": "#/backend/entities/Link" },
      "description": "Daftar tautan di dalam kelompok."
    },
    "/usernames/{username}": {
      "schema": { "type": "object" },
      "description": "Mapping username unik ke userId."
    }
  }
}
