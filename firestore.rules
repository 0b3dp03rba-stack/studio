
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Aturan paling simpel: asal login, bebas baca tulis.
      allow read, write: if request.auth != null;
    }
  }
}
