
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Memberikan akses baca publik total untuk menghindari error izin
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
